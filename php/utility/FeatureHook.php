<?php
declare(strict_types=1);

// Pokemon3d SDK utility: feature_hook

class Pokemon3dFeatureHook
{
    public static function call(Pokemon3dContext $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
